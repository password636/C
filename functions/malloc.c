/*
 * void *malloc(size_t)
 * 1. return void *, usually cast
 * 2. number of bytes
 * 3. parameter type size_t
 * 4. sizeof returns size_t: malloc( sizeof() )
 * 5. NULL on failure, check on every use
 * 6. free()
 * 7. NULL also on malloc(0)
 */
